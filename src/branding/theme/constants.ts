import { DisplayModeValueType } from "./types";

export const displayModeStorageKey: string = "uiMode";
export const displayModeStorageValues: DisplayModeValueType = {
  light: "light",
  dark: "dark",
};
